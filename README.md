# project1
you wont be able to successfully run the dbinterface node server - its configured based of the mysql server being on localhost (unless you hosted a local mysql database).
Otherwise all the code is there, free to be dissected.
